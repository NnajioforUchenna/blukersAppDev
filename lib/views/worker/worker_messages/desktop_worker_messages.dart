import 'package:flutter/material.dart';

class DesktopWorkerMessages extends StatelessWidget {
  const DesktopWorkerMessages({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 800,
      color: Colors.orange,
    );
  }
}
